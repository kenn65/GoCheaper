apiVersion: apps/v1
kind: Deployment
metadata:
  name: trips-api
  namespace: gocheaper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trips-api
  template:
    metadata:
      labels:
        app: trips-api
    spec:
      containers:
      - name: trips-api
        image: gocheaperregistry.azurecr.io/trips-api:latest
        ports:
        - containerPort: 8080
        envFrom:
        - configMapRef:
            name: gocheaper-config
        env:
        - name: Jwt__Key
          valueFrom:
            secretKeyRef:
              name: gocheaper-secrets
              key: jwt-key
        - name: ApiKey__Value
          valueFrom:
            secretKeyRef:
              name: gocheaper-secrets
              key: trips-api-key
        - name: ConnectionStrings__tripsdb
          valueFrom:
            secretKeyRef:
              name: gocheaper-secrets
              key: conn-tripsdb
        readinessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 6
        livenessProbe:
          tcpSocket:
            port: 8080
          initialDelaySeconds: 40
          periodSeconds: 30
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
